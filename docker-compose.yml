services:
  calendar-bot:
    build: .
    image: ghcr.io/akt0o/inp-calendar-bot:latest
    env_file: .env
    environment:
      DATA_DIR: /app/data
      TZ: Europe/Paris
    volumes:
      - ./data:/app/data
    restart: unless-stopped
